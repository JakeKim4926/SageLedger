[project]
name = "sageledger"
version = "0.1.0"
description = "모임 회비 장부 자동화 (카카오뱅크 거래내역 -> 장부)"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.2",
    "msoffcrypto-tool>=5.4.2",
    "PyYAML>=6.0.1",
    "python-dotenv>=1.0.0",
    "xlwings>=0.30.0",
]

[tool.uv]
package = false
